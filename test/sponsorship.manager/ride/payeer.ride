{-# STDLIB_VERSION 6 #-}
{-# CONTENT_TYPE DAPP #-}
{-# SCRIPT_TYPE ACCOUNT #-}

@Callable(i)
func paid(amount_: Int) = {
    (
        [
            ScriptTransfer(i.caller, amount_, unit)
        ], unit
    )
}